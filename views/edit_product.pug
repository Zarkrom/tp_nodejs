extends layout
block content
    h1 Modifier un produit
        form(action=`/products/${product._id}` method='put')
            input(type='hidden' name='_method' value='put')

            label(for='nom') Nom
            input#nom(type='text' name='nom' value=client.nom required)

            label(for='prix') Prix
            input#prenom(type='text' name='prix' value=client.prix required)

            button(type='submit') Mettre à jour